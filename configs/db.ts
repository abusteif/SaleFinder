export const bigw = {
    itemsTable: "id INTEGER PRIMARY KEY AUTOINCREMENT, item TEXT NOT NULL, price NUMERIC NOT NULL, date DATE",
    categoriesTable: "id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, store TEXT NOT NULL"

}